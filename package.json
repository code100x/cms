{
  "name": "cms",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "lint:fix": "eslint --max-warnings 0 --config .eslintrc.js --fix",
    "format:fix": "prettier --write \"**/*.{ts,tsx,json}\" --ignore-path ./postgres-data/",
    "format:check": "prettier --check \"**/*.{ts,tsx,json}\"",
    "dev:docker": "yarn run prisma:docker && yarn run dev",
    "prepare": "husky",
    "prisma:docker": "cd ./packages/database/ && yarn run prisma:migrate && yarn run db:seed && yarn run studio",
    "env:local": "cd apps/next-frontend/ && cp .env.example .env && cd ../../packages/database && echo 'DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/mydatabase?schema=public' > .env && cd ../../",
    "env:docker": "cd apps/next-frontend/ && cp .env.example .env && cd ../../packages/database && echo 'DATABASE_URL=postgresql://myuser:mypassword@db:5432/mydatabase?schema=public' > .env && cd ../../"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.3.2",
    "prettier-plugin-tailwindcss": "^0.6.4",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
