import { Input } from './ui/input';

export const AddPayout = () => {
  return (
    <diV>
      Add a new payout method (UPI/Solana)
      <div>
        <Input></Input>
      </div>
    </diV>
  );
};
