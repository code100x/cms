/**
 * array of public routes
 **/
export const publicRoutes = [
  '/'
]

/**
 * array of routes that need authentation
 **/
export const authRoutes = [
  "/signin"
]

/**
 * prefix for API authentation rroutes
 **/
export const apiAuthPrefix = "/api/auth"

/**
 * Default redirect path after auth..
 **/
export const DEFAULT_LOGIN_REDIRECT = "/my-courses"
